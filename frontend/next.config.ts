import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "macstore.com.pa",
      "appfobeju.com",
      "celucambio.com",
      "http2.mlstatic.com",
      "mundomac.com.ve",
    ],
  },
};

export default nextConfig;
